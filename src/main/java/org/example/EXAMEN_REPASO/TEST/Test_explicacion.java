package org.example.EXAMEN_REPASO.TEST;

import org.example.EXAMEN_REPASO.Personas;
class Test_explicacion {
    /*IMPORTANTE MOVER A CARPETA TEST DE JAVA PARA QUE FUNCIONE, SI NO NO FUNCIONARÁ


    Personas per;

    @BeforeEach
    void setUp(){
        per=new Personas(35);
    }

    @Test
    void test_persona_set_edad(){
        per.setEdad(per.getEdad()-5);
        assertEquals(per.getEdad(),30);
    }

    //HAY DIVERSOS METODOS
    // 1.- assertTrue(expresion) comprueba si la expresion da true si no dará error y fallará el test   +MUY UTILIZADO
    // 2.- assertFalse(expresion) ... lo contrario                                                      +MUY UTILIZADO
    // 3.- La Clase Assertions. (hay demasiados)
    //     Sencillamente ves mirando y segun nombre y descripcion verás que hacen
    // 4.- Assertions.assertEquals(expresion1,expresion2) comprueba que son iguales                     +MUY UTILIZADO
    // 5.- Mockito (no lo hemos dado "juraria yo")



 */
}
